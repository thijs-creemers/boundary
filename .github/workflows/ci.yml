name: Boundary CI

on:
  push:

concurrency:
  group: ${{ github.workflow }}-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

jobs:
  # =============================================================================
  # Lint all code
  # =============================================================================
  lint:
    name: Lint
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v6

      - name: Setup Clojure
        uses: DeLaGuardo/setup-clojure@13.5.2
        with:
          cli: 1.11.1.1347

      - name: Cache dependencies
        uses: actions/cache@v5
        with:
          path: |
            ~/.m2/repository
            ~/.gitlibs
          key: ${{ runner.os }}-clojure-${{ hashFiles('**/deps.edn') }}
          restore-keys: ${{ runner.os }}-clojure-
      - name: Clear clj-kondo cache
        run: rm -rf .clj-kondo/.cache/* 2>/dev/null || true

      - name: Lint with clj-kondo
        run: |
          clojure -M:clj-kondo --lint \
            boundary-tools/src \
            libs/core/src \
            libs/observability/src \
            libs/platform/src \
            libs/user/src \
            libs/admin/src \
            libs/storage/src \
            libs/cache/src \
            libs/jobs/src \
            libs/tenant/src \
            libs/email/src \
            libs/realtime/src \
            libs/workflow/src \
            libs/search/src \
            libs/external/src \
            libs/payments/src \
            libs/scaffolder/src \
            libs/calendar/src \
            libs/reports/src \
            libs/geo/src \
            libs/ai/src \
            libs/ui-style/src

  # =============================================================================
  # Build shared UI assets
  # =============================================================================
  build-ui-assets:
    name: Build UI Assets
    runs-on: ubuntu-latest
    needs: lint

    steps:
      - uses: actions/checkout@v6

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: npm

      - name: Install npm dependencies
        run: npm ci

      - name: Build admin CSS bundle
        run: npm run build:css:admin

      - name: Verify generated UI assets
        run: |
          test -f libs/ui-style/resources/public/css/daisy-admin.css
          test -f libs/ui-style/resources/public/js/theme.js
          test -f libs/ui-style/resources/public/js/htmx.min.js

  # =============================================================================
  # Documentation drift check (failing gate)
  # =============================================================================
  docs-lint:
    name: Docs Lint
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v6

      - name: Setup Clojure
        uses: DeLaGuardo/setup-clojure@13.5.2
        with:
          cli: 1.11.1.1347

      - name: Cache dependencies
        uses: actions/cache@v5
        with:
          path: |
            ~/.m2/repository
            ~/.gitlibs
          key: ${{ runner.os }}-clojure-${{ hashFiles('**/deps.edn') }}
          restore-keys: ${{ runner.os }}-clojure-

      - name: Run docs-lint
        run: clojure -M:docs-lint

      - name: Upload docs-lint report
        uses: actions/upload-artifact@v7
        if: always()
        with:
          name: docs-lint-report
          path: build/docs-lint/
          retention-days: 14

  # =============================================================================
  # AGENTS link integrity check
  # =============================================================================
  agents-links:
    name: AGENTS Links
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v6

      - name: Setup Babashka
        uses: DeLaGuardo/setup-clojure@13.5.2
        with:
          bb: latest

      - name: Check AGENTS links
        run: bb check-links

  # =============================================================================
  # Fast command smoke checks (aliases + key docs/dev commands)
  # =============================================================================
  command-smoke:
    name: Command Smoke Checks
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v6

      - name: Setup Clojure
        uses: DeLaGuardo/setup-clojure@13.5.2
        with:
          cli: 1.11.1.1347

      - name: Cache dependencies
        uses: actions/cache@v5
        with:
          path: |
            ~/.m2/repository
            ~/.gitlibs
          key: ${{ runner.os }}-clojure-${{ hashFiles('**/deps.edn') }}
          restore-keys: ${{ runner.os }}-clojure-

      - name: Setup Babashka
        uses: DeLaGuardo/setup-clojure@13.5.2
        with:
          bb: latest

      - name: Run command smoke check
        run: bb smoke-check

  # =============================================================================
  # Test individual libraries (run from root to ensure all dependencies available)
  # =============================================================================
  test-core:
    name: Test boundary/core
    runs-on: ubuntu-latest
    needs: lint

    steps:
      - uses: actions/checkout@v6

      - name: Setup Clojure
        uses: DeLaGuardo/setup-clojure@13.5.2
        with:
          cli: 1.11.1.1347

      - name: Cache dependencies
        uses: actions/cache@v5
        with:
          path: |
            ~/.m2/repository
            ~/.gitlibs
          key: ${{ runner.os }}-clojure-${{ hashFiles('**/deps.edn') }}
          restore-keys: ${{ runner.os }}-clojure-

      - name: Run tests
        env:
          JWT_SECRET: "ci-test-secret-minimum-32-characters"
        run: clojure -M:test:db/h2 :core

  test-observability:
    name: Test boundary/observability
    runs-on: ubuntu-latest
    needs: lint

    steps:
      - uses: actions/checkout@v6

      - name: Setup Clojure
        uses: DeLaGuardo/setup-clojure@13.5.2
        with:
          cli: 1.11.1.1347

      - name: Cache dependencies
        uses: actions/cache@v5
        with:
          path: |
            ~/.m2/repository
            ~/.gitlibs
          key: ${{ runner.os }}-clojure-${{ hashFiles('**/deps.edn') }}
          restore-keys: ${{ runner.os }}-clojure-

      - name: Run tests
        env:
          JWT_SECRET: "ci-test-secret-minimum-32-characters"
        run: clojure -M:test:db/h2 :observability

  test-platform:
    name: Test boundary/platform
    runs-on: ubuntu-latest
    needs: [lint, test-observability]

    steps:
      - uses: actions/checkout@v6

      - name: Setup Clojure
        uses: DeLaGuardo/setup-clojure@13.5.2
        with:
          cli: 1.11.1.1347

      - name: Cache dependencies
        uses: actions/cache@v5
        with:
          path: |
            ~/.m2/repository
            ~/.gitlibs
          key: ${{ runner.os }}-clojure-${{ hashFiles('**/deps.edn') }}
          restore-keys: ${{ runner.os }}-clojure-

      - name: Run tests
        env:
          JWT_SECRET: "ci-test-secret-minimum-32-characters"
        run: clojure -M:test:db/h2 :platform

  test-user:
    name: Test boundary/user
    runs-on: ubuntu-latest
    needs: [lint, test-platform]

    steps:
      - uses: actions/checkout@v6

      - name: Setup Clojure
        uses: DeLaGuardo/setup-clojure@13.5.2
        with:
          cli: 1.11.1.1347

      - name: Cache dependencies
        uses: actions/cache@v5
        with:
          path: |
            ~/.m2/repository
            ~/.gitlibs
          key: ${{ runner.os }}-clojure-${{ hashFiles('**/deps.edn') }}
          restore-keys: ${{ runner.os }}-clojure-

      - name: Run tests
        env:
          JWT_SECRET: "ci-test-secret-minimum-32-characters"
        run: clojure -M:test:db/h2 :user

  test-admin:
    name: Test boundary/admin
    runs-on: ubuntu-latest
    needs: [lint, test-user]

    steps:
      - uses: actions/checkout@v6

      - name: Setup Clojure
        uses: DeLaGuardo/setup-clojure@13.5.2
        with:
          cli: 1.11.1.1347

      - name: Cache dependencies
        uses: actions/cache@v5
        with:
          path: |
            ~/.m2/repository
            ~/.gitlibs
          key: ${{ runner.os }}-clojure-${{ hashFiles('**/deps.edn') }}
          restore-keys: ${{ runner.os }}-clojure-

      - name: Run tests
        env:
          JWT_SECRET: "ci-test-secret-minimum-32-characters"
        run: clojure -M:test:db/h2 :admin

  test-ui-style:
    name: Test boundary/ui-style
    runs-on: ubuntu-latest
    needs: lint

    steps:
      - uses: actions/checkout@v6

      - name: Setup Clojure
        uses: DeLaGuardo/setup-clojure@13.5.2
        with:
          cli: 1.11.1.1347

      - name: Cache dependencies
        uses: actions/cache@v5
        with:
          path: |
            ~/.m2/repository
            ~/.gitlibs
          key: ${{ runner.os }}-clojure-${{ hashFiles('**/deps.edn') }}
          restore-keys: ${{ runner.os }}-clojure-

      - name: Run tests
        env:
          JWT_SECRET: "ci-test-secret-minimum-32-characters"
        run: clojure -M:test:db/h2 :ui-style

  test-storage:
    name: Test boundary/storage
    runs-on: ubuntu-latest
    needs: [lint, test-platform]

    steps:
      - uses: actions/checkout@v6

      - name: Setup Clojure
        uses: DeLaGuardo/setup-clojure@13.5.2
        with:
          cli: 1.11.1.1347

      - name: Cache dependencies
        uses: actions/cache@v5
        with:
          path: |
            ~/.m2/repository
            ~/.gitlibs
          key: ${{ runner.os }}-clojure-${{ hashFiles('**/deps.edn') }}
          restore-keys: ${{ runner.os }}-clojure-

      - name: Run tests
        env:
          JWT_SECRET: "ci-test-secret-minimum-32-characters"
        run: clojure -M:test:db/h2 :storage

  test-cache:
    name: Test boundary/cache
    runs-on: ubuntu-latest
    needs: [lint, test-platform]

    steps:
      - uses: actions/checkout@v6

      - name: Setup Clojure
        uses: DeLaGuardo/setup-clojure@13.5.2
        with:
          cli: 1.11.1.1347

      - name: Cache dependencies
        uses: actions/cache@v5
        with:
          path: |
            ~/.m2/repository
            ~/.gitlibs
          key: ${{ runner.os }}-clojure-${{ hashFiles('**/deps.edn') }}
          restore-keys: ${{ runner.os }}-clojure-

      - name: Run tests
        env:
          JWT_SECRET: "ci-test-secret-minimum-32-characters"
        run: clojure -M:test:db/h2 :cache

  test-jobs:
    name: Test boundary/jobs
    runs-on: ubuntu-latest
    needs: [lint, test-platform]

    steps:
      - uses: actions/checkout@v6

      - name: Setup Clojure
        uses: DeLaGuardo/setup-clojure@13.5.2
        with:
          cli: 1.11.1.1347

      - name: Cache dependencies
        uses: actions/cache@v5
        with:
          path: |
            ~/.m2/repository
            ~/.gitlibs
          key: ${{ runner.os }}-clojure-${{ hashFiles('**/deps.edn') }}
          restore-keys: ${{ runner.os }}-clojure-

      - name: Run tests
        env:
          JWT_SECRET: "ci-test-secret-minimum-32-characters"
        run: clojure -M:test:db/h2 :jobs

  test-tenant:
    name: Test boundary/tenant
    runs-on: ubuntu-latest
    needs: [lint, test-platform]

    steps:
      - uses: actions/checkout@v6

      - name: Setup Clojure
        uses: DeLaGuardo/setup-clojure@13.5.2
        with:
          cli: 1.11.1.1347

      - name: Cache dependencies
        uses: actions/cache@v5
        with:
          path: |
            ~/.m2/repository
            ~/.gitlibs
          key: ${{ runner.os }}-clojure-${{ hashFiles('**/deps.edn') }}
          restore-keys: ${{ runner.os }}-clojure-

      - name: Run tests
        env:
          JWT_SECRET: "ci-test-secret-minimum-32-characters"
        run: clojure -M:test:db/h2 :tenant

  test-email:
    name: Test boundary/email
    runs-on: ubuntu-latest
    needs: [lint, test-platform]

    steps:
      - uses: actions/checkout@v6

      - name: Setup Clojure
        uses: DeLaGuardo/setup-clojure@13.5.2
        with:
          cli: 1.11.1.1347

      - name: Cache dependencies
        uses: actions/cache@v5
        with:
          path: |
            ~/.m2/repository
            ~/.gitlibs
          key: ${{ runner.os }}-clojure-${{ hashFiles('**/deps.edn') }}
          restore-keys: ${{ runner.os }}-clojure-

      - name: Run tests
        env:
          JWT_SECRET: "ci-test-secret-minimum-32-characters"
        run: clojure -M:test:db/h2 :email

  test-realtime:
    name: Test boundary/realtime
    runs-on: ubuntu-latest
    needs: [lint, test-platform]

    steps:
      - uses: actions/checkout@v6

      - name: Setup Clojure
        uses: DeLaGuardo/setup-clojure@13.5.2
        with:
          cli: 1.11.1.1347

      - name: Cache dependencies
        uses: actions/cache@v5
        with:
          path: |
            ~/.m2/repository
            ~/.gitlibs
          key: ${{ runner.os }}-clojure-${{ hashFiles('**/deps.edn') }}
          restore-keys: ${{ runner.os }}-clojure-

      - name: Run tests
        env:
          JWT_SECRET: "ci-test-secret-minimum-32-characters"
        run: clojure -M:test:db/h2 :realtime

  test-workflow:
    name: Test boundary/workflow
    runs-on: ubuntu-latest
    needs: [lint, test-platform]

    steps:
      - uses: actions/checkout@v6

      - name: Setup Clojure
        uses: DeLaGuardo/setup-clojure@13.5.2
        with:
          cli: 1.11.1.1347

      - name: Cache dependencies
        uses: actions/cache@v5
        with:
          path: |
            ~/.m2/repository
            ~/.gitlibs
          key: ${{ runner.os }}-clojure-${{ hashFiles('**/deps.edn') }}
          restore-keys: ${{ runner.os }}-clojure-

      - name: Run tests
        env:
          JWT_SECRET: "ci-test-secret-minimum-32-characters"
        run: clojure -M:test:db/h2 :workflow

  test-search:
    name: Test boundary/search
    runs-on: ubuntu-latest
    needs: [lint, test-platform]

    steps:
      - uses: actions/checkout@v6

      - name: Setup Clojure
        uses: DeLaGuardo/setup-clojure@13.5.2
        with:
          cli: 1.11.1.1347

      - name: Cache dependencies
        uses: actions/cache@v5
        with:
          path: |
            ~/.m2/repository
            ~/.gitlibs
          key: ${{ runner.os }}-clojure-${{ hashFiles('**/deps.edn') }}
          restore-keys: ${{ runner.os }}-clojure-

      - name: Run tests
        env:
          JWT_SECRET: "ci-test-secret-minimum-32-characters"
        run: clojure -M:test:db/h2 :search

  test-scaffolder:
    name: Test boundary/scaffolder
    runs-on: ubuntu-latest
    needs: [lint, test-platform]

    steps:
      - uses: actions/checkout@v6

      - name: Setup Clojure
        uses: DeLaGuardo/setup-clojure@13.5.2
        with:
          cli: 1.11.1.1347

      - name: Cache dependencies
        uses: actions/cache@v5
        with:
          path: |
            ~/.m2/repository
            ~/.gitlibs
          key: ${{ runner.os }}-clojure-${{ hashFiles('**/deps.edn') }}
          restore-keys: ${{ runner.os }}-clojure-

      - name: Run tests
        env:
          JWT_SECRET: "ci-test-secret-minimum-32-characters"
        run: clojure -M:test:db/h2 :scaffolder

  test-reports:
    name: Test boundary/reports
    runs-on: ubuntu-latest
    needs: lint

    steps:
      - uses: actions/checkout@v6

      - name: Setup Clojure
        uses: DeLaGuardo/setup-clojure@13.5.2
        with:
          cli: 1.11.1.1347

      - name: Cache dependencies
        uses: actions/cache@v5
        with:
          path: |
            ~/.m2/repository
            ~/.gitlibs
          key: ${{ runner.os }}-clojure-${{ hashFiles('**/deps.edn') }}
          restore-keys: ${{ runner.os }}-clojure-

      - name: Run tests
        env:
          JWT_SECRET: "ci-test-secret-minimum-32-characters"
        run: clojure -M:test:db/h2 :reports

  test-calendar:
    name: Test boundary/calendar
    runs-on: ubuntu-latest
    needs: lint

    steps:
      - uses: actions/checkout@v6

      - name: Setup Clojure
        uses: DeLaGuardo/setup-clojure@13.5.2
        with:
          cli: 1.11.1.1347

      - name: Cache dependencies
        uses: actions/cache@v5
        with:
          path: |
            ~/.m2/repository
            ~/.gitlibs
          key: ${{ runner.os }}-clojure-${{ hashFiles('**/deps.edn') }}
          restore-keys: ${{ runner.os }}-clojure-

      - name: Run tests
        env:
          JWT_SECRET: "ci-test-secret-minimum-32-characters"
        run: clojure -M:test:db/h2 :calendar

  test-geo:
    name: Test boundary/geo
    runs-on: ubuntu-latest
    needs: lint

    steps:
      - uses: actions/checkout@v6

      - name: Setup Clojure
        uses: DeLaGuardo/setup-clojure@13.5.2
        with:
          cli: 1.11.1.1347

      - name: Cache dependencies
        uses: actions/cache@v5
        with:
          path: |
            ~/.m2/repository
            ~/.gitlibs
          key: ${{ runner.os }}-clojure-${{ hashFiles('**/deps.edn') }}
          restore-keys: ${{ runner.os }}-clojure-

      - name: Run tests
        env:
          JWT_SECRET: "ci-test-secret-minimum-32-characters"
        run: clojure -M:test:db/h2 :geo

  test-ai:
    name: Test boundary/ai
    runs-on: ubuntu-latest
    needs: lint

    steps:
      - uses: actions/checkout@v6

      - name: Setup Clojure
        uses: DeLaGuardo/setup-clojure@13.5.2
        with:
          cli: 1.11.1.1347

      - name: Cache dependencies
        uses: actions/cache@v5
        with:
          path: |
            ~/.m2/repository
            ~/.gitlibs
          key: ${{ runner.os }}-clojure-${{ hashFiles('**/deps.edn') }}
          restore-keys: ${{ runner.os }}-clojure-

      - name: Run tests
        env:
          JWT_SECRET: "ci-test-secret-minimum-32-characters"
        run: clojure -M:test:db/h2 :ai

  # =============================================================================
  # Test external adapters
  # =============================================================================
  test-external:
    name: Test boundary/external
    runs-on: ubuntu-latest
    needs: [lint, test-platform]

    steps:
      - uses: actions/checkout@v6

      - name: Setup Clojure
        uses: DeLaGuardo/setup-clojure@13.5.2
        with:
          cli: 1.11.1.1347

      - name: Cache dependencies
        uses: actions/cache@v5
        with:
          path: |
            ~/.m2/repository
            ~/.gitlibs
          key: ${{ runner.os }}-clojure-${{ hashFiles('**/deps.edn') }}
          restore-keys: ${{ runner.os }}-clojure-

      - name: Run tests
        env:
          JWT_SECRET: "ci-test-secret-minimum-32-characters"
        run: clojure -M:test:db/h2 :external

  # =============================================================================
  # Test payments library
  # =============================================================================
  test-payments:
    name: Test boundary/payments
    runs-on: ubuntu-latest
    needs: lint

    steps:
      - uses: actions/checkout@v6

      - name: Setup Clojure
        uses: DeLaGuardo/setup-clojure@13.5.2
        with:
          cli: 1.11.1.1347

      - name: Cache dependencies
        uses: actions/cache@v5
        with:
          path: |
            ~/.m2/repository
            ~/.gitlibs
          key: ${{ runner.os }}-clojure-${{ hashFiles('**/deps.edn') }}
          restore-keys: ${{ runner.os }}-clojure-

      - name: Run tests
        env:
          JWT_SECRET: "ci-test-secret-minimum-32-characters"
        run: clojure -M:test:db/h2 :payments

  # =============================================================================
  # i18n scan gate — fails if any UI file still has unexternalised string literals
  # =============================================================================
  i18n-scan:
    name: i18n Scan
    runs-on: ubuntu-latest
    needs: lint

    steps:
      - uses: actions/checkout@v6

      - name: Setup Babashka
        uses: DeLaGuardo/setup-clojure@13.5.2
        with:
          bb: latest

      - name: Scan for unexternalised strings
        run: bb i18n:scan

  # =============================================================================
  # Test i18n library
  # =============================================================================
  test-i18n:
    name: Test boundary/i18n
    runs-on: ubuntu-latest
    needs: lint

    steps:
      - uses: actions/checkout@v6

      - name: Setup Clojure
        uses: DeLaGuardo/setup-clojure@13.5.2
        with:
          cli: 1.11.1.1347

      - name: Cache dependencies
        uses: actions/cache@v5
        with:
          path: |
            ~/.m2/repository
            ~/.gitlibs
          key: ${{ runner.os }}-clojure-${{ hashFiles('**/deps.edn') }}
          restore-keys: ${{ runner.os }}-clojure-

      - name: Run tests
        env:
          JWT_SECRET: "ci-test-secret-minimum-32-characters"
        run: clojure -M:test:db/h2 :i18n

  # =============================================================================
  # Summary job
  # =============================================================================
  test-summary:
    name: All Tests Passed
    runs-on: ubuntu-latest
    needs: [build-ui-assets, i18n-scan, test-i18n, test-core, test-observability, test-platform, test-user, test-admin, test-ui-style, test-storage, test-cache, test-jobs, test-tenant, test-email, test-realtime, test-workflow, test-search, test-scaffolder, test-external, test-payments, test-calendar, test-reports, test-geo, test-ai]

    steps:
      - name: Summary
        run: |
          echo "✅ All library tests passed!"
          echo "Libraries tested:"
          echo "  - boundary/core"
          echo "  - boundary/observability"
          echo "  - boundary/platform"
          echo "  - boundary/user"
          echo "  - boundary/admin"
          echo "  - boundary/ui-style"
          echo "  - boundary/storage"
          echo "  - boundary/cache"
          echo "  - boundary/jobs"
          echo "  - boundary/tenant"
          echo "  - boundary/email"
          echo "  - boundary/realtime"
          echo "  - boundary/workflow"
          echo "  - boundary/search"
          echo "  - boundary/scaffolder"
          echo "  - boundary/external"
          echo "  - boundary/payments"
          echo "  - boundary/calendar"
          echo "  - boundary/reports"
          echo "  - boundary/geo"
          echo "  - boundary/ai"
          echo "  - boundary/i18n"
          echo "Gates passed:"
          echo "  - i18n:scan (no unexternalised strings)"
