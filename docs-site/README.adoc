= Boundary Framework Documentation Site

This directory contains the Hugo-based documentation site for the Boundary Framework.

== Quick Start

=== Prerequisites

* Hugo Extended v0.155.3 or later
* AsciiDoctor

On macOS:

[source,bash]
----
brew install hugo asciidoctor
----

=== Initial Setup

If you've just cloned the repository, initialize the Hugo Book theme submodule:

[source,bash]
----
# From repository root
git submodule update --init --recursive
----

Or clone with submodules in one step:

[source,bash]
----
git clone --recurse-submodules <repo-url>
----

=== Development

[source,bash]
----
cd docs-site
hugo server
----

Visit http://localhost:1313/boundary/

=== Build

[source,bash]
----
cd docs-site
hugo --gc --minify
----

Output will be in `public/` directory.

== Structure

....
docs-site/
├── content/              # All documentation content
│   ├── adr/             # Architecture Decision Records
│   ├── architecture/    # Architecture guides
│   ├── guides/          # How-to guides
│   ├── api/             # API reference
│   ├── reference/       # Reference documentation
│   ├── examples/        # Example applications
│   └── changelog/       # Version history
├── themes/
│   └── hugo-book/       # Hugo Book theme (submodule)
├── static/              # Static assets
├── layouts/             # Custom layouts (if needed)
├── hugo.toml            # Hugo configuration
└── README.adoc          # This file
....

== Theme

This site uses the https://github.com/alex-shpak/hugo-book[Hugo Book] theme, which provides:

* Clean, minimal design for technical documentation
* Built-in search functionality
* Mobile responsive layout
* Native AsciiDoc and Markdown support
* Fast build times

== AsciiDoc Support

Hugo supports AsciiDoc files (`.adoc`) via the `asciidoctor` external processor.

Key configuration in `hugo.toml`:

[source,toml]
----
[security.exec]
  allow = ['^asciidoctor$', ...]

[markup.asciidocExt]
  backend = 'html5'
  workingFolderCurrent = true
----

== GitHub Pages Deployment

The site is automatically deployed to GitHub Pages via GitHub Actions when changes are pushed to the main branch.

See `.github/workflows/deploy.yml` for the deployment workflow.

== Content Guidelines

* Use AsciiDoc (`.adoc`) for all documentation content
* Each major section should have an `_index.adoc` file with Hugo frontmatter and a document title
* Follow the existing navigation structure

== Local Testing

Test the production build locally:

[source,bash]
----
hugo --gc --minify
cd public
python3 -m http.server 8000
----

Visit http://localhost:8000/boundary/

== Maintenance

=== Updating the Theme

The theme is included as a git submodule. To update to the latest version:

[source,bash]
----
# From repository root
git submodule update --remote docs-site/themes/hugo-book
git add docs-site/themes/hugo-book
git commit -m "Update hugo-book theme"
----

Or manually:

[source,bash]
----
# From repository root
cd docs-site/themes/hugo-book
git fetch origin
git checkout main
git pull
cd ../../..
git add docs-site/themes/hugo-book
git commit -m "Update hugo-book theme"
----

=== Adding New Sections

[arabic]
. Create a new directory under `content/`
. Add an `_index.adoc` file with frontmatter and document title:
+
[source,asciidoc]
----
---
title: "Section Title"
weight: 50
---

= Section Title
----
. Add content files to the directory

== Troubleshooting

=== Build fails with "`asciidoctor not found`"

Install asciidoctor:

[source,bash]
----
brew install asciidoctor  # macOS
----

=== Build fails with "`access denied: asciidoctor is not whitelisted`"

Ensure `hugo.toml` includes asciidoctor in the security allowlist:

[source,toml]
----
[security.exec]
  allow = ['^asciidoctor$', ...]
----

=== Navigation not showing

Ensure each section has an `_index.adoc` file with proper frontmatter including `weight`.

== Links

* https://gohugo.io/documentation/[Hugo Documentation]
* https://github.com/alex-shpak/hugo-book[Hugo Book Theme]
* https://gohugo.io/content-management/formats/#asciidoc[AsciiDoc in Hugo]
