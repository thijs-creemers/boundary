(ns boundary.shell.system.components.postgresql
  "Legacy PostgreSQL component stub.
   
   Note: This namespace is currently unused and retained for backward compatibility.
   Actual PostgreSQL adapter implementation is in boundary.shell.adapters.database.postgresql.*")

;; This file is a placeholder for legacy code.
;; The actual PostgreSQL database adapter is implemented in:
;; - boundary.shell.adapters.database.postgresql.core
;; - boundary.shell.adapters.database.postgresql.connection
;; - boundary.shell.adapters.database.postgresql.query
