(ns boundary.realtime.schema
  "Malli schemas for realtime module data structures.
  
  Schemas for:
  - Connection state
  - WebSocket messages
  - Authentication tokens
  - Routing metadata"
  (:require [malli.core :as m]))

;; TODO: Implement schemas in Task 5.2
;; - Connection schema
;; - Message schema
;; - QueryParams schema
