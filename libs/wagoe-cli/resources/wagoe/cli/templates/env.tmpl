# {{project-name}} — local environment variables
# Loaded by: set -a; source .env; set +a   (or direnv allow)
# Plain `source` alone does not export — child processes would not see these.
# Do NOT commit this file — it is git-ignored

# Security (required at startup, minimum 32 characters)
JWT_SECRET={{jwt-secret}}

# HTTP server
HTTP_PORT=3000
