= Project Status
:revdate: 2026-03-13
:toc: macro
:toclevels: 2

Current status snapshot for the Boundary framework monorepo.

== Summary

Boundary is at `v1.0.0-alpha` and operates as a 17-library monorepo with consistent Functional Core / Imperative Shell architecture.
All core platform capabilities are implemented. CI is comprehensive. Documentation is actively maintained.
Primary remaining work is in marked in-development libraries, not foundational architecture.

== Snapshot

[cols="1,2"]
|===
|Area |Status

|Latest release tag
|`v1.0.0-alpha` (see `CHANGELOG.md`)

|Libraries
|17 libraries in `libs/`: `core`, `observability`, `platform`, `user`, `admin`, `storage`, `scaffolder`, `cache`, `jobs`, `email`, `realtime`, `tenant`, `workflow`, `search`, `reports`, `calendar`, `external`

|Architecture model
|FC/IS enforced across all modules (`core/`, `shell/`, `ports.clj`)

|Migration stack
|Migratus (`clojure -M:migrate migrate`)

|Dev/test DB
|SQLite for development, H2 for test flows

|CI coverage
|Lint + docs lint + AGENTS links + per-library tests + full test run + summary gate
|===

== Module Status

[cols="1,2,2"]
|===
|Library |Status |Notes

|`boundary-core`
|Stable
|Validation, interceptors, utilities foundation.

|`boundary-observability`
|Stable
|Structured logging and interceptor-based instrumentation.

|`boundary-platform`
|Stable
|HTTP, database, CLI, and migration infrastructure.

|`boundary-user`
|Stable
|JWT auth, MFA, session management.

|`boundary-admin`
|Stable
|Auto-CRUD admin UI, Hiccup + HTMX.

|`boundary-storage`
|Stable
|Local and S3-style file storage.

|`boundary-scaffolder`
|Stable
|Interactive module generator (`bb scaffold`).

|`boundary-cache`
|Stable
|In-memory and Redis caching, tenant-scoped patterns.

|`boundary-jobs`
|Stable
|Background job execution with retry/queue behavior.

|`boundary-email`
|Stable
|SMTP sending, async, and jobs integration.

|`boundary-tenant`
|Active
|PostgreSQL schema-per-tenant; cross-module integration in place.

|`boundary-realtime`
|Active (single-server)
|WebSocket/JWT/pub-sub implemented. Multi-server Redis scaling is roadmap.

|`boundary-workflow`
|Stable
|State-machine workflows with permissions, guards, lifecycle hooks, auto-transitions, and audit trails.

|`boundary-search`
|Stable
|Full-text search (PostgreSQL FTS / H2 LIKE fallback), weighted fields, filter support, and admin UI.

|`boundary-reports`
|Active
|PDF, Excel, DOCX generation via `defreport` macro.

|`boundary-calendar`
|Stable
|Recurring events (RFC 5545 RRULE), iCal export/import, conflict detection, Hiccup UI components.

|`boundary-external`
|Active
|Stripe payments + webhooks, Twilio SMS/WhatsApp, SMTP transport, IMAP mailbox adapters. Not production-ready.
|===

== CI / Automation

The main CI workflow (`.github/workflows/ci.yml`) runs:

* `lint` — clj-kondo
* `docs-lint` — documentation drift linter
* `agents-links` — `bb scripts/check_agents_links.clj`
* Per-library test jobs for all 17 libraries
* Whole-suite test job (`clojure -M:test:db/h2`)
* `test-summary` gate

== Known Gaps

* `boundary-external` is explicitly in-development and not production-ready.
* `boundary-realtime` multi-server scaling (Redis-backed registry/pub-sub) is still roadmap.
* Some historical documents in `docs/archive/` are stale and should not be used as operational source-of-truth.

== Recommended Priorities

. Validate `boundary-external` adapters against live Stripe/Twilio sandboxes and mark production-ready.
. Decide realtime scaling path (single-node hardening vs Redis-backed) and align tests/docs.
. Advance `boundary-reports` and `boundary-calendar` CI coverage (add per-library jobs to CI).
. Keep CI signal quality high: fast per-library feedback and enforced docs/link hygiene.

== Maintenance Rules

* Use only verifiable repository facts (release tags, module presence, workflow definitions, docs state).
* Avoid speculative metrics unless backed by reproducible commands.
* Move historical deep-dive detail to archive docs; keep this file focused on current status.
* Update `:revdate:` on every substantive change.
